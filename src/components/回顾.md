# form的初始结构的创建

没有使用对象关系结构
单独设置 form - formItem组件;

针对表单数据维护和校验的逻辑处理设置独立usehook进行维护 ：

表单组件将大部分的处理放到了Props上

初始值的设置仅包含name- value的设置


针对再次确认密码的场景给出的方案： 
     



//Formitem中使用到的关键对象和方法 ：

     Children 是 React 提供的用于操作 props.children 的工具对象，因为 children 是一个不透明的数据结构（可能是数组、单个元素、字符串等），需要用这些工具安全地处理。

    //cloneElement ：
    cloneElement 用于克隆一个 React 元素，并可以为其添加或覆盖新的 props。

    FormItem内部控件的差异-动态适配 ：

    Checkbox checked 选中状态是布尔值
    Radio checked 同上
    Switch checked 开关状态

    useStore中使用的关键依赖库： async - validator 

     

## 可以继续补充了解的部分 ：

child的实际数据结构：
Children.toArray(children) 如何实现的扁平化 ？？

、export default memo(FormItem); 使用了memo缓存组件是否要保证props的引用不变 ？？

## 可以改进的地方 ：

手动传入内部控件的动态value和变动事件props
valuePropName="checked"
trigger="onChange"
同时获取事件触发的value ：存在变化-也要通过外部回调处理
